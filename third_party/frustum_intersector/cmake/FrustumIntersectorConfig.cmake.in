get_filename_component(FRUSTUMINTERSECTOR_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)

if(NOT TARGET FI::FrustumIntersector)
  include("${FRUSTUMINTERSECTOR_CMAKE_DIR}/FrustumIntersectorTargets.cmake")
endif()

set(FRUSTUMINTERSECTOR_LIBRARIES FI::FrustumIntersector)

